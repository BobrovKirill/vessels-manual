export const IS_DEVELOPMENT_MODE = process.env.NODE_ENV === 'development'
export const IS_PRODUCTION_MODE = process.env.NODE_ENV === 'production'
export const IS_STAGING_MODE = process.env.NODE_ENV === 'staging'
